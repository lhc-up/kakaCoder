.loadingWrap {
    display: flex;
    justify-content: center;
    padding: 30rpx 0;
}
.loading {
    animation: loading 1s linear infinite;
}
@keyframes loading {
    from {
        transform: rotate(0);
    }
    to {
        transform: rotate(360deg);
    }
}